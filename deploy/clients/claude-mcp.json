{
  "_comment": "Merge into ~/.claude.json → mcpServers (Claude Code). Replaces the old `memoryos` entry (http://astoria.local:8930/mcp/). Astoria MCP is streamable-HTTP at /mcp/ (trailing slash). Optional: add \"headers\": {\"Authorization\": \"Bearer <token>\"} once ASTORIA_CLIENT_TOKENS is set on the NAS.",
  "mcpServers": {
    "astoria": {
      "type": "http",
      "url": "http://astoria.local:8933/mcp/"
    }
  }
}
